#include <stdio.h>

int create_sort_db(double *a,int n);
int bisearching(int end_serial);

int main(void)
{
    int n;
    printf("Please tell me how many numbers would you store:");
    scanf("%d", &n);
    double a[n];
    create_sort_db(a,n);
    printf("Now please print enter your numbers:");
    sort();
    return 0;
}

int create_sort_db(double *a,int n)
{
    return;
}
int bisearching(int end_serial)
{
    return;
}
//思考如何中断输入